/*
Keyword :-
           -->  These are the reserve words for a particular programming and they are already defined in program library,
                 and these have some special meaning in program.
           --> C is very rich about their keywords ,
           ANSI provides 32 keywords for C-language four more keyword were added in c-89 version.

Main Keywords and their special features are written below in single line~~~

auto	Declares an automatic local variable.

break	Terminates a loop or switch statement.

case	Defines a branch inside a switch statement.

char	Declares a character-type variable.

const	Declares a value that cannot be modified.

continue	Skips the current loop iteration and continues with the next one.

default	Defines the default block in a switch statement.

do	Starts a do-while loop.

double	Declares a double-precision floating-point variable.

else	Executes a block when the if condition is false.

enum	Defines a set of named integer constants.

extern	Declares a variable or function defined elsewhere.

float	Declares a floating-point variable.

for	Starts a for loop.

goto	Transfers program control to a labeled statement.

if	Executes a block when a condition is true.

int	Declares an integer variable.

long	Specifies a long integer or floating-point type.

register	Suggests storing a variable in a CPU register for faster access.

return	Ends a function and optionally returns a value.

short	Specifies a short integer type.

signed	Allows an integer type to store positive and negative values.

sizeof	Returns the size of a data type or variable in bytes.

static	Preserves a variable's value between function calls or limits its scope.

struct	Defines a structure containing different types of data.

switch	Selects one block of code from multiple choices.

typedef	Creates an alternative name for a data type.

union	Defines a data type whose members share the same memory location.

unsigned	Specifies an integer type that stores only non-negative values.

void	Represents the absence of a value or data type.

volatile	Tells the compiler that a variable's value may change unexpectedly.

while	Repeats a block of code while a condition is true.

*/