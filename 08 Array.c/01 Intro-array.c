/*
Array:-
      --> It is a collection of similar types of data that stored at consecutive memory location .

      --> Array can stored several value of same type with in a single variable and allows to access 
      the data serially or sequentially. So, it is also known as Linear Data structure.

      --> Array allocates memory at compile time and each element has their unique memory location called Index or Bound.

      --> Hence Array elelment are exceed by their index value.

      --> Array data structure is used to implement tree, graph data structure .

Syntax for Array:-
                  data_type array_name[size];
                  int numbers[5];// here i am initializing an  integer array named numbers that can store 5 integers.

*/
#include <stdio.h>
void main(){
/*
 * Declaring Array :-
       General Syntax : 
            Datatype    array_name [size];
*/
    //Example

    int myArray[5];
    float scoreResult[4];

        //Initialization of Arrays
            float marks[5] = {97,98,92.97,95,96};
            char name[50] = {'r','a','t','n','e','s','h'};
}